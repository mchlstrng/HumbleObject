This pattern is particularly useful for isolating the logic that is difficult to test, such as interactions with a GUI or a device, by moving it into a ‘humble’ wrapper, while the testable logic is extracted into a separate class.